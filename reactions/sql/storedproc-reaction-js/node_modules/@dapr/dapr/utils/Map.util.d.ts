import { Map } from "google-protobuf";
import { IRequestMetadata } from "../types/RequestMetadata.type";
export declare function merge(metadataMap: Map<string, string>, metadata: IRequestMetadata): void;
