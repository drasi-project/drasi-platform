import { Registry } from "./registry";
export declare class ActivityExecutor {
    private _registry;
    constructor(registry: Registry);
    execute(orchestrationId: string, name: string, taskId: number, encodedInput?: string): Promise<string | undefined>;
}
