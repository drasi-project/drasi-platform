import * as pb from "../proto/orchestrator_service_pb";
import { Task } from "./task";
export declare class CompletableTask<T> extends Task<T> {
    constructor();
    complete(result: T): void;
    fail(message: string, details?: pb.TaskFailureDetails): void;
}
