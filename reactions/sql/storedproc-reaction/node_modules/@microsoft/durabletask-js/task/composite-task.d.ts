import { Task } from "./task";
/**
 * A task that is composed of other tasks
 */
export declare class CompositeTask<T> extends Task<T> {
    _tasks: Task<any>[];
    _completedTasks: number;
    _failedTasks: number;
    constructor(tasks: Task<any>[]);
    onChildCompleted(_: Task<any>): void;
}
