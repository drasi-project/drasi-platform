import { Endpoint } from "./AbstractEndpoint";
export declare class GrpcEndpoint extends Endpoint {
    private _authority;
    constructor(url: string);
    private preprocessUri;
    private validatePathAndQuery;
    private setTls;
    private setHostname;
    private setScheme;
    private setPort;
    private setEndpoint;
}
