import * as grpc from "@grpc/grpc-js";
import { NextCall } from "@grpc/grpc-js/build/src/client-interceptors";
import Class from "../../../types/Class";
import GRPCClient from "./GRPCClient";
export declare class GRPCClientProxy<T> {
    clsProxy: Class<T>;
    grpcClient: GRPCClient;
    grpcClientOptions: Partial<grpc.ClientOptions>;
    constructor(clsProxy: Class<T>, grpcClient: GRPCClient, grpcClientOptions?: Partial<grpc.ClientOptions>);
    generateInterceptors(): ((options: grpc.InterceptorOptions, nextCall: NextCall) => grpc.InterceptingCall)[];
    build(): Promise<T>;
}
