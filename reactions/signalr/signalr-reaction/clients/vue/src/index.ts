import { App } from 'vue'
import ResultSet from './components/ResultSet.vue'

function install(app: App) {
  //app.component('ResultSet', ResultSet)
}

export default { install }

export { ResultSet }
